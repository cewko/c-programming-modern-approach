/*
    ++i -> increments first, result is the incremented value
    i++ -> result is the old value, increment happens after 

    i += 1 -> incresements first, result is increment value

    i+= 1 == ++i
*/