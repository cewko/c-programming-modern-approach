/*
    why is it not a good idea for an identifier to contain 
    more than one adjacent underscore (as in current___balance, for example)?

    because they are hard to read or count, also this kind of names
    can conflict with the c standard identifies like __func__, __restrict, and others
*/


